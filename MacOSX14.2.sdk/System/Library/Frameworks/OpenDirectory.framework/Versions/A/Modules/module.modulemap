framework module OpenDirectory [system] {
  umbrella header "OpenDirectory.h"

  export *
  module * { export * }

  exclude header "NSOpenDirectory.h"

  framework module CFOpenDirectory [system] {
    umbrella header "CFOpenDirectory.h"
    export *
    module * { export * }
  }
}
